import { HighScore, highScoreList } from '../model';
import { addToScore, resetScore,
  addHighScore, getHighScoreList, resetHighScoreList  } from '../index'
import { storage, Context } from 'near-sdk-as'

// describe('Greeting ', () => {
//   it('should be set and read', () => {
//     setGreeting('hello world')
//     storage.get<string>(Context.sender)
//   })
// })

//TODO write tests
